name: bigfoot
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  firebase_core: ^2.10.0
  cloud_firestore: ^4.5.2
  flutter_slidable: ^3.0.0
  firebase_ui_firestore: ^1.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: Abril Fatface
      fonts:
        - asset: fonts/Abril_Fatface/AbrilFatface-Regular.ttf
